name: Backend Docker Build

on:
  push:
    branches:
      - main
      - feature/backend-gym-crud

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t gym-backend .
