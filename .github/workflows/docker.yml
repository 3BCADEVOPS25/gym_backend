name: Backend Docker Build

on:
  push:
    branches:
      - main
      - feature/backend-gym-crud

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker Image
        run: |
          cd gym_backend/gym/gym
          docker build -t gym-backend .
